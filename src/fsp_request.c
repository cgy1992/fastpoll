#include "app.h"
#include "fsp_request.h"

void* fsp_thrd_req_manage(struct fsp_app* app)
{
  return app;
}

void* fsp_thrd_req_serve(struct fsp_app* app)
{
  return app;
}

void* fsp_thrd_req_cleanup(struct fsp_app* app)
{
  return app;
}
